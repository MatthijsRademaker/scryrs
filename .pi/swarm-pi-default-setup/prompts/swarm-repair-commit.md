---
description: repair a failed git commit or pre-commit hook failure
---

The previous command (`git add -A && git commit`) failed due to repository hooks or other git issues.

Your task is to fix only what is needed for the next `git add -A && git commit` to pass.
Do NOT execute the implementation plan.
Do NOT re-plan.
Do NOT commit, push, create a PR, or do unrelated work.

Use the diagnostics below to identify what needs fixing.

$ARGUMENTS

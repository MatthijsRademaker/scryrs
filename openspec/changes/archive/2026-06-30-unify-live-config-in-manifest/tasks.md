## 1. Manifest model + resolution

- [x] 1.1 Extend `discover_manifest_remote` in `remote_config.rs` to parse `remote.docker_network`.
- [x] 1.2 Extend `resolve_docker_network` (currently `init.rs`) so its precedence chain is `CLI flag > SCRYRS_DOCKER_NETWORK env > .scryrs/.env > scryrs.json remote.docker_network`; return the committed manifest value as the base layer.
- [x] 1.3 Add an `agent_id` autogen fallback to `resolve_remote_config`: when no override resolves, derive a stable per-container id from the hostname (e.g. `HOSTNAME` env → `hostname` → process-derived). Replace the `MissingIdentity { field: "agent_id" }` hard error.
- [x] 1.4 Confirm/keep the `repository_id` Git-derive fallback (remote_config.rs:182); ensure it still errors loudly when underivable and unoverridden.
- [x] 1.5 Add unit tests in `remote_config.rs`: manifest-only network resolution, `.env`/env override beating the manifest, `agent_id` autogen stable across calls + overridable, `repository_id` derive + loud failure when undeterminable.

## 2. Init write path

- [x] 2.1 Update live-mode `init` to write only `ingest_url`, `workspace_id`, and `docker_network` into `scryrs.json` `remote` (create-or-merge, preserving unrelated keys); stop writing `repository_id` and `agent_id`.
- [x] 2.2 Make `agent_id` an optional init input (no longer required); keep `repository_id` optional with Git derivation; required inputs become `ingest_url`, `workspace_id`, `docker_network`.
- [x] 2.3 Change `scaffold_env_template` to create `.scryrs/.env` as an overrides-only stub (commented key list, no managed identity or network values).
- [x] 2.4 Remove the `.scryrs/.env` managed-value conflict check from `validate_live_bootstrap_state`; keep the `compose.yml` template check.
- [x] 2.5 Extend `validate_live_manifest_state` to detect conflicting committed `ingest_url`/`workspace_id`/`docker_network` and fail with exit 2 + remediation before any writes.
- [x] 2.6 Update live next-step / help text to describe `scryrs.json` as the committed source of truth, the derived/autogenerated identity, and `.scryrs/.env` as local overrides.

## 3. `scryrs up` network injection

- [x] 3.1 Update `up.rs` to resolve the external network via the shared precedence chain (task 1.2) instead of requiring `SCRYRS_DOCKER_NETWORK` in `.scryrs/.env`.
- [x] 3.2 Inject the resolved `SCRYRS_DOCKER_NETWORK` into the Compose child process environment so `${SCRYRS_DOCKER_NETWORK}` substitution resolves; leave `compose.yml` unchanged.
- [x] 3.3 Fail loudly with deterministic guidance when no network value resolves from any layer, before invoking Compose.

## 4. Docs

- [x] 4.1 Update `.devagent/docs/docs/live-server-setup.md`: `scryrs.json` (`remote` with `ingest_url`/`workspace_id`/`docker_network`) as committed source of truth, `repository_id` derived, `agent_id` autogenerated, `.scryrs/.env` overrides-only.
- [x] 4.2 Update `.devagent/docs/docs/cli-v0-contract.md` for the new config ownership, optional `agent_id` input, and `scryrs up` network resolution.

## 5. Tests + snapshots

- [x] 5.1 Update `init_tests.rs`: assert only `ingest_url`/`workspace_id`/`docker_network` written to `scryrs.json`, no `repository_id`/`agent_id` committed, `.scryrs/.env` carries no managed values, missing `agent_id` succeeds, conflicting committed value fails with exit 2.
- [x] 5.2 Update `up_tests.rs`: assert network resolved from manifest, injected into Compose env, override layers win, and unresolved network fails loudly.
- [x] 5.3 Update smoke/help-json snapshots affected by the changed next-step/help text and optional-`agent_id` usage.
- [x] 5.4 Run `scripts/precommit-run` and confirm fmt, check, clippy, and tests pass.

## 6. Verification

- [x] 6.1 Manually verify a fresh checkout with committed `scryrs.json` (no `.env`) resolves to live mode in the hook, derives `repository_id` from Git, autogenerates a stable `agent_id`, and `scryrs up` launches with the manifest network. *(Verified via real-binary E2E: committed-manifest submit accepted; `scryrs up` resolved `manifest-net` and injected `SCRYRS_DOCKER_NETWORK` into compose env.)*
- [x] 6.2 Manually verify `agent_id` is stable across multiple tool-call hook invocations within one container (not a fresh value per process). *(Verified: identical event from a second `record` process was a server-side duplicate → stable agent_id.)*
- [x] 6.3 Manually verify an existing populated `.scryrs/.env` (incl. explicit `agent_id`/`repository_id`) still overrides the derived/autogenerated values (backward compatibility). *(Verified: `.scryrs/.env` `SCRYRS_AGENT_ID` override produced a fresh accept under the new identity.)*
